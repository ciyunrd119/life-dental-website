# 專案維護規則

## 牙醫知識元件庫同步

- 修改 `knowledge/*.html` 內可重複使用的文章區塊、HTML 結構或 `know-*` 類別語法時，必須同步更新根目錄的 `element.html`。
- 修改 `css/style.css` 中牙醫知識文章相關樣式，或 `js/main.js` 中牙醫知識文章互動時，也必須檢查並同步更新 `element.html` 的預覽、範例程式碼與 CSS 類別速查。
- 單純替換個別文章的文案、日期、圖片或連結，不需要更新 `element.html`，除非因此新增或改變了可重用元件語法。
- 完成相關修改後，需確認 `element.html` 的實際預覽與可複製程式碼一致，且桌機與手機版皆沒有橫向溢位。
- `element.html` 是內部參考頁，不得加入網站主選單。
